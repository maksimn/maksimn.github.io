var SHRI = (function () {
    function shri() {
    }

    shri.prototype.createTeam = function(name) {
    }

    shri.prototype.getTeam = function (name) {
        return { name: 'team1' };
    }

    return shri;
})();